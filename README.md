# Вайб-кодинг — Платформа курса

Образовательная платформа для курса "Вайб-кодинг: с нуля до своего проекта".

## 🚀 Быстрый старт

### Локальный запуск

```bash
cd course-platform
npm install
npm start
# Открыть http://localhost:3000
```

### Деплой на GitHub Pages

```bash
# 1. Создайте репозиторий на GitHub

# 2. Инициализируйте git и запушьте
cd /home/ubuntu/project/Vibecoder
git init course-platform-repo
cd course-platform-repo
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/YOUR_USERNAME/course-platform.git
git push -u origin main

# 3. Настройте GitHub Pages:
#    Settings → Pages → Source: GitHub Actions
```

После пуша сайт автоматически обновится!

## 📁 Структура проекта

```
course-platform/
├── index.html              # Главная страница (SPA)
├── exercises.html          # Страница упражнений
├── favicon.svg             # Иконка
├── package.json            # Зависимости
├── server.js               # Node.js сервер (для локальной разработки)
├── .gitignore
├── .github/
│   └── workflows/
│       └── deploy.yml      # CI/CD для GitHub Pages
├── css/
│   ├── style.css           # Основные стили
│   └── markdown.css        # Стили для материалов
├── js/
│   ├── app.js              # Логика приложения
│   └── data.js             # Работа с localStorage
├── data/
│   └── modules.json        # Метаданные модулей
└── course-materials → ../../course/materials  # Симлинк на материалы
```

## 🎨 Особенности

- **Статический сайт** — работает без сервера
- **Прогресс сохраняется** — в localStorage браузера
- **Адаптивный дизайн** — работает на мобильных
- **Markdown рендеринг** — материалы отображаются красиво
- **Автодеплой** — при пуше в main сайт обновляется

## 📚 Материалы курса

Материалы берутся из папки `course/` в корне проекта:

```
course/
├── materials/              # Теория модулей
├── exercises/             # Практические задания
└── solutions/             # Ответы к упражнениям
```

## ⚙️ Настройка

### Цвета
Редактируйте `css/style.css`:
```css
:root {
    --primary: #6366f1;      /* Основной цвет */
    --success: #22c55e;      /* Цвет успеха */
}
```

### Модули
Редактируйте `data/modules.json`:
```json
{
  "id": "01",
  "title": "Название модуля",
  "exercises": 2
}
```

## 🛠️ Разработка

```bash
# Запуск с логами
npm start

# Проверка markdown
# Откройте: http://localhost:3000/course-materials/module-01-environment.md
```

## 📝 Лог изменений

См. `PROJECT_LOGS.md`

## 📄 Лицензия

MIT
